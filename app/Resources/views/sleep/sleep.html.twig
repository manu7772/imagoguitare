<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8" />
	<title>Site en maintenance</title>
	<style type="text/css" src="http://fonts.googleapis.com/css?family=Raleway:400,100,200,300,500,600,700,800,900"></style>
	<style type="text/css">
		body {
			font-family: 'Raleway', sans-serif;
			letter-spacing: 1px;
		}
		h1, h2, h3, p {
			text-align: center;
		}
		.spacer-200 {
			height: 200px;
		}
	</style>
</head>
<body>
	<div class="spacer-200"></div>
	{% if mode == 'lancement' %}
		<h1>Site en préparation</h1>
		<h3>Vous ne pouvez accéder au site pour le moment</h3>
		{% if duration|default(null) != null %}
		<p>Le site sera accessible à partir du {{ dateFR(duration) }} à {{ duration|date('H:i') }}</p>
		{% else %}
		<p>La date de lancement n'est pas encore définie.</p>
		{% endif %}
	{% else %}
		<h1>Site en maintenance</h1>
		<h3>Vous ne pouvez accéder au site pour le moment</h3>
		{% if duration|default(null) != null %}
		<p>Le site sera à nouveau accessible à partir du {{ dateFR(duration) }} à {{ duration|date('H:i') }}</p>
		{% else %}
		<p>La maintenance est prolongée pour une période indéterminée.</p>
		{% endif %}
	{% endif %}
</body>
</html>